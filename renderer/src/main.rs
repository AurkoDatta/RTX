//! CLI entry point. The full scene-JSON-in, progressive-frames-out protocol is
//! wired up in a later phase; for now this just confirms the binary builds and
//! links against the library crate.

fn main() {
    println!("renderer: CLI not yet implemented");
}
